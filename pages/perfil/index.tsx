import PerfilTemplate, { Profile } from "@/components/pages/Perfil";

export default function PerfilPage() {
    return (
        <PerfilTemplate>
            <Profile />
        </PerfilTemplate>
    );
}
